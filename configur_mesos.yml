- name: Install and configure mesos
  hosts: mesos
  become: true
  user: ansible
  gather_facts: no
  become_method: sudo
  vars_files:
   - group_vars/all
  roles:
   - role: prelimnary
   - role: hardening
   - role: mesos
   - role: cleanup
